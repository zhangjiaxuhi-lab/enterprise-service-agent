# 密钥扫描放行规则（可选）
#
# 每行一条**正则**，匹配仓库内的文件路径。匹配到的文件将被密钥扫描跳过
# （本地 pre-commit 钩子与 CI 的 secret-scan workflow 均生效）。
#
# 使用原则：这是误报的正式出口，**不是**放过真实密钥的后门。
#   * 优先修文件（把示例改成 your_key_here / sk-xxxxxxxx 这类明显占位形式）；
#   * 只有当文件确实包含"看起来像密钥但其实是示例"的内容、且不宜修改时，
#     才在此登记。
#   * 真实密钥一律不得在此登记——正确做法是吊销密钥并从文件中移除。
#
# `#` 开头为注释，空行忽略。
#
# 示例（按需取消注释）：
# ^docs/.*\.md$
# ^tests/fixtures/secrets\.txt$
